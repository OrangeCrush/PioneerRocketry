<h2>Pioneer Rocketry Members</h2></br>
<b> There <%= @users.size > 1 ? 'are' : 'is' %> currently <%= @users.size %> member<%=@users.size > 1 ? 's' : nil %> signed up.</b>
<ol>
   <% @users.each do |user|  %>
      <li> <%= user.email %><li>
<% end %>
</ol>
